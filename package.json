{
  "name": "monitoring-saas",
  "version": "1.0.0",
  "description": "A full-stack monitoring SaaS application designed to track uptime, SSL certificates, and system health. The app provides real-time monitoring, job queue processing, and a dashboard to visualize metrics. Built with a modern stack for scalability and extensibility.",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "env:create": "node scripts/createEnv.js",
    "deps:install": "npm install --prefix backend && npm install --prefix monitoring-frontend",
    "redis:start": "docker run -d --name monitoring-redis -p 6379:6379 redis:7",
    "redis:stop": "docker stop monitoring-redis && docker rm monitoring-redis"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "web-push": "^3.6.7"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
