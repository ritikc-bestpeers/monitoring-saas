name: Greetings

on: [pull_request_target, issues]

jobs:
  greeting:
    runs-on: ubuntu-latest
    permissions:
      issues: write
      pull-requests: write
    steps:
    - uses: actions/first-interaction@v1
      with:
        repo-token: ${{ secrets.GITHUB_TOKEN }}
        issue-message: |
            Hi there! 👋
            Thank you for opening your first issue in this repository. We appreciate your contribution!
        pr-message: |
            Hi there! 👋
            Thank you for opening your first pull request in this repository. We look forward to reviewing it!
